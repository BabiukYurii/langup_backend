# PaymentsConfig: per-provider credentials and webhook secrets:
# Stripe (api key, webhook secret), PayPal (client id/secret), Google Pay,
# BLIK/PSP (merchant id, key). Trial length, currency, retry policy.
