# CaptureRequest: structured JSON emitted by the browser extension.
# Fields: word, sentence, surrounding_context, page_url, page_title,
# page_html (optional), language, dom_path, timestamp.
# CaptureResponse: created word/context ids.
