# CaptureService: entry point for browser-extension payloads.
# Normalizes word, persists source + context, enqueues AI context analysis,
# creates/updates the user's UserWord and seeds its SRS schedule.
